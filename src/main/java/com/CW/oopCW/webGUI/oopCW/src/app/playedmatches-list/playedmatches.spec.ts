import { Playedmatches } from '../playedmatches-list/playedmatches';

describe('Playedmatches', () => {
  it('should create an instance', () => {
    expect(new Playedmatches()).toBeTruthy();
  });
});
